# DB 설정 (Docker 컨테이너)
DB_USERNAME=flow
DB_PASSWORD=flow123

# JWT 시크릿 (운영환경용 강력한 키 사용)
JWT_SECRET=your-256-bit-secret-key-for-production

# 프론트엔드 API 설정 (비워두면 nginx 프록시 사용)
API_URL=
WS_URL=/ws

# 이메일 설정
# dev: 콘솔에 인증코드 출력, prod: AWS SES로 실제 이메일 발송
EMAIL_ENVIRONMENT=prod
AWS_SES_REGION=ap-northeast-2
AWS_SES_SENDER=noreply@yourdomain.com

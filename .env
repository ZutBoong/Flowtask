# Flowtask 환경 설정
# 로컬: 그대로 사용
# AWS: EMAIL_ENVIRONMENT=prod 로 변경

# DB 설정
DB_USERNAME=flow
DB_PASSWORD=flow123

# JWT 시크릿
JWT_SECRET=FlowtaskProjectSecretKeyForJWTTokenGeneration2024VeryLongSecretKey

# 프론트엔드 API 설정
API_URL=
WS_URL=/ws

# 이메일 설정 (dev=콘솔출력, prod=실제발송)
EMAIL_ENVIRONMENT=dev

# AWS SES 설정 (prod 모드에서만 사용)
AWS_SES_REGION=ap-northeast-2
AWS_SES_SENDER=noreply@flowtask.com
